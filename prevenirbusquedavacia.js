window.addEventListener("load", function () {

  document.querySelector(".form-buscador button").addEventListener("click", function (event) {
    event.preventDefault()
    // 1) capturo todos los campos del formulario
    // 2) Valido que x lo menos un campo este completo
    // 3) Si esto sucede lo dejo submitir, sino alerto que tiene que buscar algo al usuario
    
  })
})
